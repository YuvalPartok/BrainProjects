from tkinter import *
from front.front import MyWindow

window = Tk() # creates a new window
mywin = MyWindow(window)
window.title("BRAINPUT")
window.geometry("600x350") # sets the size of the window
window.mainloop() #
